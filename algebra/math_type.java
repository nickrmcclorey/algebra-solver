package algebra;

/* 
 * number represents a numeric literal like 4, 3, 8.2 etc...
 * symbol is an unknown variable like x, y, z
 * parent means the Expression is made of two Expressions
 */
public enum math_type { number, symbol, parent }
